/**
 * Brand color system for DeliveryHub.
 *
 * Derived from the approved UI reference (soft-lavender canvas, deep indigo
 * accent). Centralised here and consumed via `useAppTheme()`.
 */
export { brandColors, type BrandColorTokens } from './palette';